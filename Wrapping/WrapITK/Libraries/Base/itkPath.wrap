WRAP_CLASS("itk::Path" POINTER_WITH_EXPLICIT_SPECIALIZATION)
  foreach(d ${WRAP_ITK_DIMS})
    WRAP_TEMPLATE("${ITKM_D}${ITKM_CID${d}}${d}" "${ITKT_D}, ${ITKT_CID${d}}, ${d}")
  endforeach(d)
END_WRAP_CLASS()
