WRAP_CLASS("itk::NodePair")
  foreach(d ${WRAP_ITK_DIMS})
    foreach(t ${WRAP_ITK_SCALAR})
      WRAP_TEMPLATE("I${d}${ITKM_${t}}"  "itk::Index<${d}>, ${ITKT_${t}}")
    endforeach(t)
  endforeach(d)
END_WRAP_CLASS()
