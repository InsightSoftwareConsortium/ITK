WRAP_CLASS("itk::HistogramToProbabilityImageFilter" POINTER_WITH_SUPERCLASS)
  FOREACH(d ${WRAP_ITK_DIMS})
    FOREACH(t ${WRAP_ITK_REAL})
      WRAP_TEMPLATE("${ITKM_HF}${ITKM_I${t}${d}}"  "${ITKT_HF}, ${ITKT_I${t}${d}}")
      WRAP_TEMPLATE("${ITKM_HD}${ITKM_I${t}${d}}"  "${ITKT_HD}, ${ITKT_I${t}${d}}")
    ENDFOREACH(t)
  ENDFOREACH(d)
END_WRAP_CLASS()
