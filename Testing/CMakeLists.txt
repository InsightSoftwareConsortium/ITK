
IF(ITK_WRAP_TCL)
  SET(ITKTCL_EXECUTABLE "${ITK_BINARY_DIR}/Wrapping/Tcl/Executable/itktcl")
ENDIF(ITK_WRAP_TCL)

IF(BUILD_TESTING)
SUBDIRS( Code )
ENDIF(BUILD_TESTING)
LINK_LIBRARIES(
  VXLNumerics
)
IF(UNIX)
  LINK_LIBRARIES (
  -lm
  )
  IF (NOT ITK_USE_SYSTEM_STDLIB)
  LINK_LIBRARIES (
  -lCio
  )
  ENDIF (NOT ITK_USE_SYSTEM_STDLIB)
ENDIF(UNIX)

