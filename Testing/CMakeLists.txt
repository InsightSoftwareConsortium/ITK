OPTION(BUILD_TESTING "Build the testing tree for Insight." "On")

IF(BUILD_TESTING)
SUBDIRS( Code HTML )
ENDIF(BUILD_TESTING)

LINK_LIBRARIES (
VXLNumerics
)

IF(UNIX)
  LINK_LIBRARIES (
  -lm
  )
ENDIF(UNIX)

