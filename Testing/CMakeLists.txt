
IF(BUILD_TESTING)
SUBDIRS( Code )
ENDIF(BUILD_TESTING)
LINK_LIBRARIES(
  VXLNumerics
)
IF(UNIX)
  LINK_LIBRARIES (
  -lm
  )
  IF (NOT ITK_USE_SYSTEM_STDLIB)
  LINK_LIBRARIES (
  -lCio
  )
  ENDIF (NOT ITK_USE_SYSTEM_STDLIB)
ENDIF(UNIX)

