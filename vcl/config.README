The purpose of the config.$build_tag directories is to hold
files generated by the configure script. Apart from platforms
where configure is not used (e.g. Windows) these directories
are generated and thus should not be under CVS or svn control.
