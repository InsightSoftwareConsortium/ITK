itk_wrap_class("itk::MaskedRankImageFilter" POINTER_WITH_SUPERCLASS)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${WRAP_ITK_SCALAR})
      foreach(l ${WRAP_ITK_INT})
        itk_wrap_template("${ITKM_I${t}${d}}${ITKM_I${l}${d}}${ITKM_I${t}${d}}${ITKM_SE${d}}"    "${ITKT_I${t}${d}},${ITKT_I${l}${d}},${ITKT_I${t}${d}},${ITKT_SE${d}}")
      endforeach(l)
    endforeach()
  endforeach()
itk_end_wrap_class()
