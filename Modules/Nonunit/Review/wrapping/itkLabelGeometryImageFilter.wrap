itk_wrap_class("itk::LabelGeometryImageFilter" POINTER)
foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t1 ${WRAP_ITK_SCALAR})
      foreach(t2 ${WRAP_ITK_USIGN_INT} ${WRAP_ITK_SIGN_INT})
        itk_wrap_template("${ITKM_I${t2}${d}}${ITKM_I${t1}${d}}" "${ITKT_I${t2}${d}},${ITKT_I${t1}${d}}")
      endforeach()
    endforeach()
  endforeach()
itk_end_wrap_class()
