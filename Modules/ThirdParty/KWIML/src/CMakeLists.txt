set(KWIML_INSTALL_INCLUDE_DIR ${ITKKWIML_INSTALL_INCLUDE_DIR})
set(KWIML_INSTALL_INCLUDE_OPTIONS COMPONENT Development)
set(KWIML_INCLUDE_PREFIX itkkwiml)
if(BUILD_TESTING)
  set(KWIML_TEST_ENABLE 1)
  set(KWIML_TEST_PREFIX itkkwiml)
endif()
add_subdirectory(itkkwiml)
