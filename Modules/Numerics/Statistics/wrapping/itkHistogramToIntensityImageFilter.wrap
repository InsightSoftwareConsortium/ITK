itk_wrap_class("itk::HistogramToIntensityImageFilter" POINTER_WITH_SUPERCLASS)
  FOREACH(d ${ITK_WRAP_DIMS})
    FOREACH(t ${WRAP_ITK_REAL})
      itk_wrap_template("${ITKM_HF}${ITKM_I${t}${d}}"  "${ITKT_HF}, ${ITKT_I${t}${d}}")
      itk_wrap_template("${ITKM_HD}${ITKM_I${t}${d}}"  "${ITKT_HD}, ${ITKT_I${t}${d}}")
    ENDFOREACH(t)
  ENDFOREACH(d)
itk_end_wrap_class()
