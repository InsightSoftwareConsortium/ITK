itk_wrap_class("itk::HistogramToProbabilityImageFilter" POINTER_WITH_SUPERCLASS)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${WRAP_ITK_REAL})
      itk_wrap_template("${ITKM_HF}${ITKM_I${t}${d}}"  "${ITKT_HF}, ${ITKT_I${t}${d}}")
      itk_wrap_template("${ITKM_HD}${ITKM_I${t}${d}}"  "${ITKT_HD}, ${ITKT_I${t}${d}}")
    endforeach()
  endforeach()
itk_end_wrap_class()
