itk_wrap_class("itk::ObjectToObjectMetric" POINTER)
  foreach(d ${ITK_WRAP_DIMS})
    foreach(d2 ${ITK_WRAP_DIMS})
      itk_wrap_template("${d}${d2}" "${d},${d2}")
    endforeach()
  endforeach()
itk_end_wrap_class()
