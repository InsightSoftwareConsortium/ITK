itk_wrap_class("itk::BayesianClassifierInitializationImageFilter" POINTER)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${WRAP_ITK_SCALAR})
      foreach(t3 ${WRAP_ITK_REAL})
        itk_wrap_template("${ITKM_I${t}${d}}${ITKM_${t3}}" "${ITKT_I${t}${d}}, ${ITKT_${t3}}")
      endforeach()
    endforeach()
  endforeach()
itk_end_wrap_class()
