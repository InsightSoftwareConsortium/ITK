itk_wrap_class("itk::RawImageIO" POINTER)
  UNIQUE(to_types "UC;${WRAP_ITK_SCALAR}")
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${to_types})
      itk_wrap_template("${ITKM_${t}${d}}${ITKM_${t}${d}}" "${ITKT_${t}${d}},${ITKT_${t}${d}}")
    endforeach()
  endforeach()
itk_end_wrap_class()
