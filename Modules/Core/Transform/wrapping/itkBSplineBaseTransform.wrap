itk_wrap_class("itk::BSplineBaseTransform" POINTER)
  UNIQUE(transform_type "D;${WRAP_ITK_REAL}")
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${transform_type})
      itk_wrap_template("${ITKM_${t}}${d}3" "${ITKT_${t}},${d},3")
    endforeach()
  endforeach()
itk_end_wrap_class()
