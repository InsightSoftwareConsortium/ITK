set(types "${WRAP_ITK_REAL}")
set(WRAPPER_AUTO_INCLUDE_HEADERS OFF)
itk_wrap_include("itkGaussianDerivativeOperator.h")
itk_wrap_simple_class("itk::GaussianDerivativeOperatorEnums")
itk_wrap_class("itk::GaussianDerivativeOperator")
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${types})
      itk_wrap_template("${ITKM_${t}}${d}" "${ITKT_${t}}, ${d}")
    endforeach()
  endforeach()
itk_end_wrap_class()
