itk_wrap_include("itkPointSet.h")
itk_wrap_include("itkDefaultStaticMeshTraits.h")

UNIQUE(types "${WRAP_ITK_SCALAR};D")

itk_wrap_class("itk::ExpectationBasedPointSetToPointSetMetricv4" POINTER)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${types})
      itk_wrap_template("PS${ITKM_${t}}${d}" "itk::PointSet< ${ITKT_${t}},${d} >")
    endforeach()
  endforeach()
itk_end_wrap_class()

itk_wrap_class("itk::RegistrationParameterScalesFromPhysicalShift" POINTER_WITH_2_SUPERCLASSES)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${types})
      itk_wrap_template("EBPSTPSMPS${ITKM_${t}}${d}"
        "itk::ExpectationBasedPointSetToPointSetMetricv4< itk::PointSet< ${ITKT_${t}},${d} > >")
    endforeach()
  endforeach()
itk_end_wrap_class()
