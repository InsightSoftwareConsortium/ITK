UNIQUE(types "D;${WRAP_ITK_REAL}")
itk_wrap_class("itk::LandmarkBasedTransformInitializer" POINTER)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${types})
      itk_wrap_template("${ITKM_I${t}${d}}" "itk::Transform<${ITKT_${t}},${d}>")
    endforeach()
  endforeach()
itk_end_wrap_class()
