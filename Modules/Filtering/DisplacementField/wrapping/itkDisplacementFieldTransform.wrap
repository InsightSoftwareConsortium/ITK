itk_wrap_class("itk::DisplacementFieldTransform" POINTER)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    itk_wrap_template("${ITKM_D}${d}" "${ITKT_D},${d}")
    if (ITK_WRAP_vector_float)
      itk_wrap_template("${ITKM_F}${d}" "${ITKT_F},${d}")
    endif()
  endforeach()
itk_end_wrap_class()
