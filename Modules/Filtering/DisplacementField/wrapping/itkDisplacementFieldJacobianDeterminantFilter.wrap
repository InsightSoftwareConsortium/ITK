itk_wrap_class("itk::DisplacementFieldJacobianDeterminantFilter" POINTER)
  foreach(d ${ITK_WRAP_DIMS})
    if(d GREATER 0 AND d LESS 5)
      foreach(v ${WRAP_ITK_VECTOR_REAL})
        foreach(r ${WRAP_ITK_REAL})
          itk_wrap_template("${ITKM_I${v}${d}${d}}${ITKM_${r}}" "${ITKT_I${v}${d}${d}}, ${ITKT_${r}}")
        endforeach(r)
      endforeach(v)
    endif(d GREATER 0 AND d LESS 5)
  endforeach()
itk_end_wrap_class()
