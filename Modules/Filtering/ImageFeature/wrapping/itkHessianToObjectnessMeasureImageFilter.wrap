itk_wrap_class("itk::HessianToObjectnessMeasureImageFilter" POINTER)
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${WRAP_ITK_SCALAR})
      itk_wrap_template("${ITKM_ISSRTD${d}${d}}${ITKM_I${t}${d}}"  "${ITKT_ISSRTD${d}${d}}, ${ITKT_I${t}${d}}")
    endforeach()
  endforeach()
itk_end_wrap_class()
