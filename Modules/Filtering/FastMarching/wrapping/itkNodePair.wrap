itk_wrap_class("itk::NodePair")
  foreach(d ${ITK_WRAP_IMAGE_DIMS})
    foreach(t ${WRAP_ITK_SCALAR})
      itk_wrap_template("I${d}${ITKM_${t}}"  "itk::Index<${d}>, ${ITKT_${t}}")
    endforeach()
  endforeach()
itk_end_wrap_class()
