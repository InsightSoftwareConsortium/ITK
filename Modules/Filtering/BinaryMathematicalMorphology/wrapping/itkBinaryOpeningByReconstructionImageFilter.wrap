itk_wrap_include("itkFlatStructuringElement.h")
itk_wrap_class("itk::BinaryOpeningByReconstructionImageFilter" POINTER)
  itk_wrap_filter_dims(restricted_dims 2+)
  foreach(d ${restricted_dims})
    foreach(t ${WRAP_ITK_USIGN_INT})
      itk_wrap_template("${ITKM_I${t}${d}}${ITKM_SE${d}}" "${ITKT_I${t}${d}},${ITKT_SE${d}}")
    endforeach()
  endforeach()
itk_end_wrap_class()
