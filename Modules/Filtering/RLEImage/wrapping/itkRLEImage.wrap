itk_wrap_class("itk::RLEImage" POINTER)
foreach(d ${ITK_WRAP_IMAGE_DIMS})
  foreach(t ${WRAP_ITK_SCALAR})
    itk_wrap_template("${ITKM_${t}}${d}US" "${ITKT_${t}},${d},unsigned short")
  endforeach()
endforeach()
itk_end_wrap_class()
