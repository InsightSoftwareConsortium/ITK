itk_wrap_class("itk::StatisticsLabelMapFilter" POINTER)
  foreach(d ${ITK_WRAP_DIMS})
    foreach(t2 ${WRAP_ITK_SCALAR})
      itk_wrap_template("${ITKM_LM${d}}${ITKM_I${t2}${d}}" "${ITKT_LM${d}}, ${ITKT_I${t2}${d}}")
    endforeach(t2)
  endforeach(d)
itk_end_wrap_class()
