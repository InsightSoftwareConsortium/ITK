itk_wrap_class("itk::LabelStatisticsKeepNObjectsImageFilter" POINTER)
  foreach(d ${ITK_WRAP_DIMS})
    foreach(t2 ${WRAP_ITK_SCALAR})
      foreach(t1 ${WRAP_ITK_USIGN_INT})
        itk_wrap_template("${ITKM_I${t1}${d}}${ITKM_I${t2}${d}}"  "${ITKT_I${t1}${d}},${ITKT_I${t2}${d}}")
      endforeach(t1)
    endforeach(t2)
  endforeach(d)
itk_end_wrap_class()
